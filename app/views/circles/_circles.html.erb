<% if circles.any? %>
  <ul class="media-list">
    <% circles.each do |circle| %>
      <li class="media">
        <%#<div class="media-left">
          <img class="media-object img-rounded" src="<%= gravatar_url(user, { size: 50 }) %><%#" alt="">
        </div>%>
        <div class="media-body">
          <div>
            <%= circle.name %>
          </div>
          <%#<div>
            <p><%= link_to 'View profile', circle_path(circle) %><%#</p>
          </div>%>
        </div>
      </li>
    <% end %>
  </ul>
  <%= paginate circles %>
<% end %>